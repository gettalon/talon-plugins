# Workflow Definition — {{WORKFLOW_NAME}}
# Generated by agent-factory
# Validate with: manage-workflow.sh validate <this-file>

name: "{{WORKFLOW_NAME}}"
description: "{{WORKFLOW_DESCRIPTION}}"
initial_state: "{{INITIAL_STATE}}"

states:
  {{INITIAL_STATE}}:
    name: "{{INITIAL_STATE}}"
    on_entry: "{{INITIAL_STATE_INSTRUCTIONS}}"
    transitions:
      - to: "{{NEXT_STATE}}"

  {{NEXT_STATE}}:
    name: "{{NEXT_STATE}}"
    transitions:
      - to: done

  done:
    name: done
    on_entry: "Work complete. Present results."
    transitions: []
